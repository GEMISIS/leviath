name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

jobs:
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: rustfmt,clippy
      - uses: Swatinem/rust-cache@v2
      - name: Build
        run: cargo build --all-targets
      - name: Test
        run: cargo test
      # GitHub's native per-job `badge.svg?job=...` filter was observed to
      # render a "passing" badge even for a job name that doesn't exist in
      # this workflow at all -- it silently falls back to some other status
      # rather than erroring, so it can't be trusted to reflect this specific
      # job's real per-OS result. Publish it ourselves instead, via the same
      # self-published-gist mechanism already proven for the coverage badge
      # below: `job.status` reflects the real accumulated pass/fail state of
      # this job's steps so far, checked with `if: always()` so it runs
      # (and reports failure) even when the Test step above failed.
      - name: Publish test status badge
        if: always() && github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: schneegans/dynamic-badges-action@v1.7.0
        with:
          auth: ${{ secrets.GIST_TOKEN }}
          gistID: ${{ vars.COVERAGE_GIST_ID }}
          filename: leviath-test-${{ matrix.os }}.json
          label: ${{ matrix.os }}
          message: ${{ job.status == 'success' && 'passing' || 'failing' }}
          color: ${{ job.status == 'success' && 'brightgreen' || 'red' }}

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings

  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: rustfmt
      - name: Check formatting
        run: cargo fmt --all -- --check

  coverage:
    name: Coverage (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: llvm-tools,rustfmt,clippy
      - uses: Swatinem/rust-cache@v2
      - name: Install cargo-llvm-cov
        run: cargo install cargo-llvm-cov --locked
      - name: Check coverage suppression markers
        run: cargo xtask check-exclusions
      # Branch coverage is not collected -- `cargo llvm-cov --branch`
      # reliably crashes with SIGSEGV on an open, unresolved upstream LLVM
      # bug (see the doc comment at the top of xtask/src/coverage.rs).
      # Coverage enforcement IS active: this step fails the build if missed
      # regions/lines/functions exceed the locked-in ceiling in
      # xtask/src/coverage.rs (MAX_MISSED_REGIONS/LINES/FUNCTIONS) -- see
      # that file's doc comments for why literal 100% isn't the target
      # (individually investigated, confirmed-permanent tooling limitations)
      # and how the ceiling should be lowered as real gaps get closed. This
      # step computes and reports all three metrics (regions/lines/functions)
      # so re-publishing the other two as badges later is a config-only
      # change -- only the lines badge is published for now (see the step
      # below) since three separate coverage numbers read as confusing to
      # newcomers; regions/functions stay computed but unpublished.
      - name: Compute coverage (regions / lines / functions)
        id: cov
        run: cargo xtask coverage
      # Publish the single line-coverage badge — only from ubuntu on push to
      # main (all three OSes compute coverage; only one need publish it).
      - name: Update coverage badge
        if: matrix.os == 'ubuntu-latest' && github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: schneegans/dynamic-badges-action@v1.7.0
        with:
          auth: ${{ secrets.GIST_TOKEN }}
          gistID: ${{ vars.COVERAGE_GIST_ID }}
          filename: leviath-coverage-lines.json
          label: coverage
          message: ${{ steps.cov.outputs.lines }}%
          valColorRange: ${{ steps.cov.outputs.lines }}
          minColorRange: 50
          maxColorRange: 100
