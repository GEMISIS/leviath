flowchart LR
    collect(collect)
    prioritize{{prioritize}}
    brief(brief)
    summary(summary)
    collect --> prioritize
    prioritize --> brief
    prioritize --> collect
    brief --> summary
    classDef entry stroke-width:3px
    class collect entry
