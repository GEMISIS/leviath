flowchart TD
    discover(discover)
    validate(validate)
    parallel_fix(parallel fix)
    fix_worker(fix worker)
    merge_fixes(merge fixes)
    verify(verify)
    discover --> validate
    validate --> parallel_fix
    parallel_fix ==>|×5 workers| fix_worker
    fix_worker ==>|merge| merge_fixes
    merge_fixes --> verify
    verify --> validate
    classDef entry stroke-width:3px
    class discover entry
