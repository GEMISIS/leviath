flowchart LR
    scope{{scope}}
    split(split)
    gather_worker(gather worker)
    build(build)
    present(present)
    scope --> split
    scope --> build
    split ==>|×4 workers| gather_worker
    gather_worker ==>|merge| build
    build --> present
    classDef entry stroke-width:3px
    class scope entry
